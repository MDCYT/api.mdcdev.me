components:
  schemas:
    V1DiscordInviteChannel:
      type: object
      required:
        - createdAt
        - createdAtTimestamp
        - id
        - name
        - type
      properties:
        createdAt:
          type: string
          format: date
          description: The date and time when the channel was created.
        createdAtTimestamp:
          type: integer
          description: The timestamp of the date and time when the channel was created.
        id:
          type: string
          description: The snowflake for the channel.
        name:
          type: string
          description: The name of the channel.
        type:
          type: integer
          description: The type of the channel.