/v1/users/@me:
  get:
    summary: Get my Discord user info
    description: Get the info about my Discord user
    tags: [V1 Discord Users]
    responses:
      200:
        description: The info about the my Discord user
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/V1DiscordUser'