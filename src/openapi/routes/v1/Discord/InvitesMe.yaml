/v1/invites/@me:
  get:
    summary: Get my Discord server invite info
    description: Get the info about my server invite
    tags: [V1 Discord Invites]
    responses:
      200:
        description: The info about the my server invite code
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/V1DiscordInvite'